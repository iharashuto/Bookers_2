<div class="container">
  <div class="row">
  	<% if @book.errors.any? %>
<div id ="error_explanation">
<h2><%= @book.errors.count%> errors prohibited this obj from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
  </ul>
</div>
<% end %>
    <div class="col-xs-3">
       <%= render "users/userinfo",user: current_user %>
       <%= render "books/new",book: @book %>
	</div>
	  <div  class="col-xs-9">
		<h2>Books</h2>
		<table class="table">
		  <thead>
			<tr>
			  <th></th>
	  		  <th>Title</th>
	  		  <th>Opinion</th>
			</tr>
			</thead>
			<% @books.each do |book| %>
			<tbody>
		 	  <tr>
		       <td><%= link_to(user_path(book.user), class: "") do %>
		       	<%= attachment_image_tag book.user, :profile_image, :fill,70,70,format:"jpg",fallback: "no_image.jpg", size:'40x40'  %>
		       	<% end %>
		       	</td>
				<td><%= link_to book.title, book_path(book.id) %></td>
				<td><%= book.body %></td>
			  </tr>
			</tbody>
			<% end %>
			</table>
		</div>
	   </div>
	</div>